# Icons

The UI currently uses text symbols and emoji so it does not depend on a large icon library. Add any future icon assets here and keep them decorative with `aria-hidden="true"` when they do not convey unique information.
